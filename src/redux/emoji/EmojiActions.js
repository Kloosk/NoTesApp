import {EMOJI_CLOSE,EMOJI_OPEN} from "./EmojiTypes";

export const emojiClose = () => {
    return{
        type: EMOJI_CLOSE
    }
};
export const emojiOpen = () => {
    return{
        type: EMOJI_OPEN
    }
};