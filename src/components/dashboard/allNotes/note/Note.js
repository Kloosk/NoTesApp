import React from 'react';
import styled from 'styled-components'

const Container = styled.div`

`;
const Note = ({}) => {
    return (
        <Container>

        </Container>
    );
};

export default Note;